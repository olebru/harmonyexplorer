<div class="pt-3 grid grid-cols-7">                         
        @foreach (var headerText in headerValues)
        {
                <button class="text-left font-bold bg-blue-200 text-blue-900 py-0 px-0 w-full focus:outline-none text-xs md:text-sm lg:text-base">@headerText</button>
        }
</div>            
        @foreach (var modeList in data)
        {      
                <button class="text-left font-bold bg-blue-100 text-blue-500 py-1 px-0 w-full focus:outline-none text-xs md:text-sm lg:text-base">@modeList[0].Mode</button>
        
                @foreach(var extension in upperExtensions)
                {
                        @foreach (var modeWithChords in modeList)
                        { 
                                <div class = "grid grid-cols-7">
                                        @foreach (var chord in modeWithChords.Chords)
                                        {
                                                <ChordViewComponent chord = "@chord" extension = "@extension"/>
                                        }
                                </div>                             
                        }
                }
        }  
<div class="text-blue-500 text-right px-1 py-2 text-xs">
        Preview, work in progress. <a href="https://github.com/olebru/harmonyexplorer">Github</a> - Ole Idar Bruvik 2020
</div>